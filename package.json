{
  "name": "job-star",
  "version": "1.0.0",
  "description": "행정업무 자동화 및 상담 지원 웹 애플리케이션",
  "main": "local-server.js",
  "scripts": {
    "start": "node local-server.js",
    "dev": "node local-server.js",
    "test": "node --test",
    "check:config": "node --env-file-if-exists=.env.local scripts/check-config.js"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "oauth4webapi": "^3.8.8"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": "22.x"
  }
}
